# {节点名}

> **区域静态档案（只读·世界初始设定·人工维护）**。本文件是**设定源**不是状态：只写初始事实（结构/方位/连接/环境/常驻/法则/功能），**永不改**（与 SETTING.md / CONFLICTS_SEED.md 同级·设定·不改）。谁在、道具移动、解锁状态等一切演进 → 动态层（world_map / scene_state / CHAR_state）。有 `regions/` 目录即激活本增强层；缺失不影响运行。模板/规则详见 references/scene_management.md §区域静态档案。

## 属性
- 类型: {EXT / INT / 移动}
- 安全等级: {高 / 中 / 低 / 极危}（可选）
- 解禁条件: {无 / 具体事件或道具}（可选）
- 对应节点: {world_map 节点名（目录名与 world_map 节点名不同时写明映射，如 旧城区 → Old Town）}（可选）

## 方位
{相对父区域的静态方位}

## 相邻区域
- {邻居名}（{方位/联通方法描述}）

## 环境与氛围
{物理环境/光线/气味/天气——静态事实}

## 常驻NPC
{CHAR_xxx 指针 / 无}

## 本地法则
{仅本区域生效的特殊规则 / 无}

## 叙事锚点
{剧情功能——写功能不写进度}

## 子区域
- {子节点名}（{联通方法/关系描述}——目录树=结构权威·不重复列路径）
- {子节点名}（未建档·按需补建）
