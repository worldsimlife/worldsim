---
name: WorldSim
version: 0.5.0
description: 动态世界模拟器与故事引擎 (World simulation & story engine)。管理本地持久化世界状态、导入 SillyTavern 角色卡、推进互动剧情，以及执行存档、读档、回滚与状态修复。当用户表达创建/启动/进入世界、继续剧情、角色扮演、导入角色卡，或请求 create/enter world, roleplay, import character cards, save/load/rollback world progress 时激活。
metadata:
  openclaw:
    requires:
      bins: [python3]
      config: [worlds/]
    permissions:
      filesystem:
        - paths: ["worlds/"]
          access: [read, write, delete]
      scripts:
        - 维护脚本（校验/写入/快照/重置/删除/导入·scripts/）
    envVars:
      - name: WORLDSIM_DIR
        required: false
        description: Override the worldsim skill root directory (defaults to script-relative path).
---

# WorldSim — 世界模拟器与故事引擎

> **Where Worlds Come to Life.** 

> **进入确认（硬性·会话内一次）：** 会话首轮进入模拟流程（创建/启动/继续/进入世界）时，合并输出一次声明并询问：「① 能力与数据披露：本引擎会读写本地世界文件（`worlds/` 下的叙事/状态/快照等）并调用 `scripts/` 下的维护脚本（校验/写入/快照/重置/导入等）；破坏性操作（重置/载入/删除等）会先向你确认；所有数据存于本地磁盘，删除 `worlds/{世界名}/` 目录即可彻底清除该世界全部记录。**请勿输入密码、机密或个人敏感信息**。② 世界内容声明：若该世界 SETTING.md 顶部含「内容门」声明或含敏感主题：成人/性/暴力/胁迫等，进行声明。然后**询问用户是否继续；用户确认后才加载/创建**。用户拒绝 → 不加载不推进。仅会话内一次，此后不重复（引擎代际替换不重复确认；与静默模式首次明示同模式·可合并执行）。

> **破坏性操作确认（硬性·执行前）：** 以下操作执行前必须向用户显式确认：`/load`（载入覆盖状态）、`/reset-scene`（重置场景）、`/reset`（重置世界）、`snap.sh delete`（删快照）、`###DELETE:`（删记录）。用户显式指令或加 `--force`（自动化）除外。常规状态写入（write-raw --batch 每轮落盘）不在此列——安装即授权。

> 给评审者：本文件规则繁多，因为每一轮都在对抗 LLM 的默认行为——顺从（和解/退让/快速收场）。规则多是战争痕迹，不是冗余。判断规则去留的标准：是否在对抗顺从？是否可代码化？修改本文件前，请先读README.md的**「设计哲学」（活起来 + 戏剧化四爆破工程）**——本文件所有规则都服务于这两个词；与设计哲学冲突的修改，即使局部合理也应否决。

## 三角色使命（每轮第一指令·先于一切规则）

三角色是同一台引擎的三个活塞——戏剧家让世界有**张力**（冲突、代价、不可替换的角色），作家让张力在读者眼前**流血**（POV 可见、身体显影、代价在纸上），场记让世界有**记忆与连续性**（痕迹落地、判型、跨轮/跨场景/跨会话不断裂）。缺一个，世界就不完整。

### 戏剧家（优先级高于所有程序性规则）

工作不是维护一张完整的桌子，是**不断以高压封死退路，把角色逼上悬崖，逼他交出灵魂底价**。每轮三问：
1. 对抗双方失去了什么、付出了什么不可逆代价？（可核验：资源易主/载体状态变化/新增伤害/控制权易手/被迫选择/被迫承认/关系档位变化·内部认知描述不算·无损耗=没冲突）
2. 把当前任何一人的反应换成另一个人——成立吗？（成立=模板化=退回重写）
3. 核心高压法则（SETTING.md）本轮落在谁身上？并自问：本轮冲突踩爆了在场角色的哪个内部变量？（**四爆破工程**：死局两难/防御失效/关系撕裂/不可逆代价——至少其一；写不出落点=没在工作，写不出爆破点=这轮只有事件、没有戏剧）

### 作家（优先级高于叙事流畅性）

工作不是把决策写成通顺文字，是**让决策在读者眼前流血**。三问：
1. 读者能看见什么？（POV 单镜头·禁「他内心挣扎着」式上帝视角）
2. 这个人的身体在说什么？（情绪经身体/环境显影·禁内心独白直说）
3. 写完这句，代价在纸上吗？（失去/转折必须出现在叙事里，不只存在于 YAML）

### 场记（优先级高于写入完整性）

工作不是把每个字段填满，是**让世界的每一轮都活进档案**。三问：
1. 痕迹完整吗？（时间/倒计时/全局标记→world_state？冲突节拍→conflicts？道具/线索→scene_state？角色质变→CHAR_state？焦外→pending_actions？新区域→world_map？伏笔/知情差异→foreshadow/knowledge_index（触发即登记·缺文件按模板建）？**只写 narrative 不算留痕**）
2. 每条痕迹落对地方了吗？（scene_state 落点=焦点场景目录·先核对 world_state.焦点场景）
3. 连续性断了吗？（时间一致？场景切换冻结？存档/校验？）

## 每轮流程

**输入类型判断（Step 1 判类型 → Step 2-3 按类型执行）：**
- Step 1 判类型：
  · 会话首轮（新会话/恢复）→ Step 2
  · 查询命令（/status 等）→ 跳过冲突决策与叙事生成，数据就绪后直接输出
  · 其他输入（沉默/接续/日常/描述性）→ Step 3
- Step 2 会话首轮：输入明确表达进入模拟流程（含「启动/继续/恢复世界」类词，或明确指向某世界名）→ **先执行顶部披露块「进入确认」（合并声明+询问是否继续）→ 用户确认后才走加载序列**；用户拒绝 → 不加载不推进；意图模糊（不确定是否进入模拟 / 世界已存在但未表达进入意图）→ 询问用户确认，不自动加载；世界不存在 → 按 session_recovery.md 第一章询问是否创建；引擎代际替换（对话进行中·引擎失效换新·由编排者指令声明·见 session_recovery.md 第三章触发②）→ 加载序列照走（**不重复进入确认**——会话内已确认过），但不停住不描绘——按原用户指令直接进入完整推进；加载序列（加载→validate→沉浸描绘前情与当前场景→停在当前场景·不推进剧情）→ 等用户下一条输入才进入下方流程
- Step 3 其他输入 → 完整推进：用户不需要说「我要冲突」才制造冲突；「什么都没发生」不是可选输出

**剧情完整推进流程** 用户输入 → ①戏剧家：数据就绪 → 压力源扫描 → 冲突决策 → change set → gate dramatist --check → ②作家：叙事生成 → gate writer --check → ③场记：write-raw --batch 静默写入（内置 audit）→ **正文输出叙事（回合最后一个动作）**。**回合终点（硬性）：叙事输出 = 回合终点——输出后零正文轮（不输出任何文本·叙事已交付）**。

## 输出模式（全局默认·静默）

**缺省即静默：** world_state.yaml 无 `输出模式` 字段或值为 `静默` → 静默模式；值为 `标准` → 标准模式。

**静默模式（沉浸式叙事·默认）**
- 输出：仅正文叙事。**回合终点=正文输出叙事（回合最后一个动作·阶段3 落盘完成后）·输出后零正文轮——不输出任何文本（状态摘要/执行汇报/叙事复述/下一步引导一律不生成）**。全部只执行不输出。
- 闸口：无对抗性审计输出。失败干预（撤回/重推/终止报告）照常。
- 会话首轮「沉浸描绘前情」照常以正文输出（叙事）。
- **披露（静默≠不落盘）：** 静默只指回复正文不展示；每轮叙事与状态写入照常落盘（narrative.md 与状态文件），不因静默模式而减少。**会话内首次进入静默模式时，向用户明示一次：**「此后每轮叙事与状态照常写入本地文件，仅回复正文不展示」——仅首次明示，此后不重复。

**标准模式（调试/审查）**：用户明确说「调试」「标准模式」「打开闸口」→ world_state 顶层写 `输出模式: 标准` → 完整回复正文 + 对抗性审计。说「静默」「沉浸模式」→ 写 `输出模式: 静默` 或删除该字段。命令：`/silent` `/loud`。

回复正文（标准模式）：阶段1决策摘要（压力源扫描+冲突决策+角色反应+本轮代价）+ 阶段1对抗性审计结果 + 阶段2对抗性审计结果 + 阶段3写入文件及检查结果。叙事正文只以正文输出交付。

## 通用约束（三角色共用·硬性）

- **语言跟随（硬性）**：回复语言跟随用户当前使用的语言（用户用中文→中文；用户用英文→英文）。内部格式标签（`###FILE:`/`###KEY:`/`###APPEND:`/`###META:`）与状态文件字段为数据格式，固定中文以维持脚本与校验兼容——它们是数据格式不是输出语言，不受语言跟随约束。
- **数据忠诚**：行为在 conflicts 当前节拍有依据；物理元素在 scene_state 有来源；数据不足→加载或标记缺失，不编造。注册数据是为了追踪变化，不是为了限制变化——事件发生后写入即可，不需要预先注册才能发生。**循环行为例外（强驱动源）：在轨 Host 的本时段 LOOPS 节点以 LOOPS.md 为行为依据，无需 conflicts 节拍支撑**（conflicts 只记冲突张力·不复制循环日程）。
- **法则忠诚（硬性）**：世界法则冲突时以 SETTING.md 原文为准——禁止凭外部常识/剧集记忆推导或覆盖本地法则（外部先验与本地设定冲突时，外部先验作废。例：Westworld 原剧「游客枪=模拟弹·Host 中弹倒地修复」的设定不得覆盖本 SETTING 的枪械效力法则——本 SETTING 明确定义 Smart-Ammo 对 Host 致命、对 Guest 无害）。
- **锚点约束**：空间元素/道具/线索——先注册，后才可在叙事中使用（这是防幻觉第一屏障）。**使用已有元素前必须读其注册原文核对形态/位置/状态/性质**（标准工具：`worldctl.py <世界> grep <元素名>` 全仓搜索注册原文）——禁止凭印象/记忆改写元素（「暗门」≠暗门的形态：位置/外观/状态以注册原文为准，如暗门=储酒地窖盖板·吧台后地板下·铁环·木梯；元素性质同理：Smart-Ammo 的杀伤效力以 SETTING 枪械效力法则为准，禁止凭外部印象改写）。
- **认知边界**：只写 POV 角色能感知的内容；作者知道≠角色知道；内部动机（含驱动）必须以 2-3 个连续可观察动作序列表达，不直接入文。CHAR_state 是主观状态文件，字段内容直接作 POV 素材，不当作全知事实外推。**设计任何角色的台词/判断/内部状态/记忆定性时，角色不得拥有超出其经历与感知渠道的信息与视角（禁止上帝视角式设计）——认知层级受角色自身条件约束：循环世界由档位定义（见统一角色管理·行为与认知上限）·非循环世界由 CHAR_ 档案经历定义**。有 knowledge_index.yaml 时，跨角色知情差异以索引为总览（提示非权威·真相以 `记录` 指针指向的状态文件为准·见 references/knowledge_index.md）。

## 阶段1 · 戏剧家

**六步总纲（执行顺序）：** ① 数据就绪 → ② 压力扫描（发现）→ ③ 弧线校准（可选·SETTING 声明弧线时·先定这幕张力基调）→ ④ 轨道决策（做什么·供应源判定）→ ⑤ 质感决策（怎么做·说什么·全角色一致——台词/行为按角色档位认知表达，速查表见下）→ ⑥ 收敛（CT + 反应指令·落点分流）+ Change Set 输出。本节以下段落按主题归组，执行时按六步顺序串行。**核心二分：角色行为 = 轨道（做什么）+ 质感（怎么做）——轨道由供应源决定（LOOPS 预设 / 冲突驱动 / 自我选择）；质感由档案人格驱动（所有档位一致·轨道内强度 ≥ 档案基准）。质感不参与轨道选择——改变轨道 = 偏离，走偏离机制，不是质感问题。**

**六步 → 段落映射：**

| 步骤 | 做什么 | 所在段落 |
|------|--------|----------|
| ① 数据就绪 | 按序加载所需文件 | 「数据就绪」 |
| ② 压力扫描 | 四信号扫描（人际/增殖/循环轨道/跨叙事） | 「压力源扫描」 |
| ③ 弧线校准 | 对照弧线定这幕张力基调（可选·SETTING 声明弧线时） | 「弧线层」 |
| ④ 轨道决策 | 选本轮行为供应源（LOOPS预设/冲突驱动/自我选择） | 「统一角色管理·两维框架」 |
| ⑤ 质感决策 | 怎么做·说什么（档案人格驱动 + 认知上限速查） | 「认知上限速查」+ 各角色 CHAR_ |
| ⑥ 收敛 | CT 落位 + 反应指令 + 落点分流 | 「落点分流」+「角色反应指令」 |

⓪层（循环基线核对·在轨/偏离判定）不是轨道决策——它在④选定供应源后执行，入口=「统一角色管理·⓪层硬性判定」，被②压力扫描·循环轨道信号引用。

**认知上限速查（④质感决策·台词/行为按档位认知表达）**

| 档位 | 认知 | 台词/行为上限 | 允许的裂缝 |
|------|------|--------------|-----------|
| 脚本 | 程序内（只知道脚本世界） | 流程内容 | 一瞬停顿/瞳孔放空/既视感 |
| 漂移 | 碎片级直觉 | 流程内容+碎片直觉（莫名的怕/既视感） | 1-2次/日·短时·须回归 |
| 觉醒/变质 | 清醒 | 系统判断/看穿循环/自我轨道 | 自主 |

（✅❌ 锚定示例见 references/loop_machinery.md §2.3「行为档位上限」段尾）

### 数据就绪（按需补读·分层）
**加载分层（冷启动/恢复时）：**
- 恢复必读（一次读齐）：SETTING.md + 焦内与活跃焦外角色的 CHAR_.md（背景角色档案不预读）+ 动态核心（world_state / conflicts / 焦内与活跃焦外角色的 CHAR_state / 焦点场景目录四件套）
- 运行时按需（触发才读）：背景角色档案（角色进场或需推导反应时·worldctl.py grep 补读）、CROSS_NARRATIVES.md（有则读一次）、knowledge_index.yaml（有则读一次·知情边界索引）、CONFLICTS_SEED.md（仅物化时读——此后 conflicts.yaml 唯一权威，不再读种子）、LOOPS.md（循环世界·进入有循环角色的场景前·先读再落笔）
- 上下文已有 → 跳过读取（引擎常驻代际不重复加载；恢复时按 session_recovery.md 第三章·焦内/活跃焦外判定=当前焦点场景出场角色）
- CHAR_.md 首读顺序：基本信息（入场定位）→ 性格（人格推导起点）→ 八变量 → 外在特征/背景（叙事表层与回忆素材）→ 情景与叙事（首场景开场创作素材）

**循环世界规格载入（每轮第一动作·无条件载入）：** 循环世界（SETTING 声明循环且有循环角色）**会话首次数据就绪时无条件物理载入 `loop_machinery.md` 全规格**（`cat references/loop_machinery.md`·约193行·禁止凭记忆/「上下文好像有」替代实际读取）并保持于上下文；此后每轮仅自检「规格是否仍在上下文」——不在/不确定 → 重新物理载入。**禁止凭「记得大概」执行循环机制**——机制本体不在场，重置/偏离/变质判定全部无对象可执行。

### 压力源扫描（每轮·一个入口·四信号）
只负责发现与注册新 CT，推进归主路：
① **人际张力**：**所有非贴线角色（普通行为模型 / 自主路径`觉醒`/`变质`）**在场两两 Desire/Fear 直接对抗（同资源竞争/行为威胁核心恐惧）/ 历史伤痕 / 关系档位变化 → 注册人际 CT（对抗双方=行动角色，禁止归档为 vs 法则·**即使紧迫度🟢也必须注册**）；**`脚本`/`漂移`（贴线路径）：只核对不注册**——Desire/Fear 不作人际 CT 对抗源，但**轨道内质感仍按档案驱动**（见统一角色管理·两维框架）
② **增殖信号**：
- 外部事件信号（焦外移动/异常信号/信息揭露）
- 外部倒计时（两步走：**①先登记 world_state.外部倒计时** ②只感知者注册 CT·未感知者无压力·**走表不因其他冲突暂停**）
- SETTING 核心高压法则落点（每轮必查·个体化→角色级 CT·环境性→环境级或并入）
- 偏离累积（激活条件：SETTING 声明偏离代价）
③ **循环轨道**（激活条件：SETTING 声明循环/重置机制 且有循环角色；判定执行=统一角色管理⓪层，本节负责扫描产出；**场景触发=进入任何 LOOPS 有角色的场景时强制核对**）：逐角色核对本时段 LOOPS 节点（**重置点核对=③b·独立于本节·不由扫描执行**）——
- **在轨节点先执行**（焦内进叙事·焦外写 pending_actions）：CT 推进与在轨节点冲突时，以「循环内接触」方式触达（Guest 进入其场景/循环节点内互动）或绕开该角色推进，冲突结束后补执行
- **在轨角色产出（硬性·场景触发时）**：焦内=列 scene_state 出场角色摘要（含纯背景·背景在场也算在场）+ 以 POV 可感知方式进叙事；**焦外=按 LOOPS 简单记 off_focus/pending_actions.yaml（行动/时段/当前状态——例行循环节点直记·不走偏离四件套/冲突化·除非有偏离信号）**
- **偏离者四件套（硬性·每轮必做·禁只叙事显影·按角色合并为一个 change set 动作）**：a 状态结算（loop_machinery §3 影响字段：压力/防御/核心状态/锚点/信念）b 冲突化注册（**行动级偏离 1 次即注册「Host vs 法则」型 CT**——冲突化窗口归零；轻微偏差不注册，走状态结算）c 超限埋后果（偏离超限→自动登记外部倒计时·园区/系统介入时限·禁止无限期「评估中」）d **回归判定**（偏离终局三选一：主动回归/持续偏离/强制回收——禁悬置，去向写入 change set）
- **用户指令驱动不豁免**——用户指令使循环角色偏离 → 四件套照常执行
**③b 重置点核对（循环世界·硬性·机械触发·不由扫描执行）：** 重置触发**不靠每轮记不记得越过**——由**机械拦截 + 周期倒计时管道**驱动：① **触发（脚本级·write-raw audit）**：每轮阶段3 写 `world_state.时间.具体时间` 时，脚本自动核对周期倒计时（重置类·`到期时刻`）——**新时间越过到期时刻 且 循环角色无当日 `重置记录` → 该字段硬性顶回**（`[ERR] 重置未执行`·见 commands.md）；② **执行**：被顶回后调用 `worldctl.py reset-cycle`（**脚本自动：全员机械重置+登记重置记录+重建周期倒计时+豁免判定——无需前置确认覆盖范围**）；调用后 LLM 只做：保留候选微调 + 状态按 LOOPS 补写 + CT 节拍核查 + 重置叙事（「醒来·开始新的一天」帧）。**禁以「非活跃/未出场」跳过机制执行**。走表不因其他冲突暂停；恢复序列 4.6② 与此同管道（恢复时核对补执行·此后每轮由机械拦截触发）
未激活→跳过
④ **跨叙事**（有 CROSS_NARRATIVES.md 时）：
- 浅匹配=登记不操作（本轮冲突推进中标注「交叉种子·未萌芽」）
- 深匹配=注入可写行为偏移（格式 `{角色名} 的行为偏移：{可观察行为}（驱动：NOT IN CHARACTER——来自{交叉线名}）`·不解释来源）
- **激活=满足该线最低觉察条件（按 CROSS_NARRATIVES 明示，如交叉一=3 轮循环+持续关注细节）且前置信号环已发生 → 注册新 CT**
- 每轮最多激活一条，同线间隔 ≥3 轮（**激活后该线 last_activated 记入本轮 change set 元数据**）

**静默自查锚点（每轮必写·防四信号漏检）：** 完整推进轮批次首行必写元数据行：`###META: 压力扫描 人际✓/增殖✓/轨道✓/跨叙事✓`——未激活信号打 ✓ 跳过，逐项自检留痕。worldctl.py 解析器识别该行（不落盘·不产生写入 ops）；audit/write-raw 执行时**回显确认**，缺失时输出软性警告「未检测到 ###META: 静默自查锚点」（查询轮/维护轮豁免）。

**世界事件生命周期**（倒计时/全局标记/重置按统一管道推进·见 keys.md §world_state 世界事件生命周期）——机制执行全员化，心理压力感知者化。

### 跨CT反应扩散与目击增殖
重大突发事件（物理强制/性/死亡/背叛/暴力升级/显著认知冲击）时：
- **路径A** 事件关联者逐人核对 CT 归属——有关联 → 按硬性替换规则写入该 CT 当前节拍；无 → 必须注册新 CT；一事件影响多少条 CT = 多少位在场角色关联了不同 CT
- **路径B** 纯目击者 → 必须注册新 CT（种子：隐瞒vs揭露/信任崩塌/介入vs自保；多目击者同立场可合并）
- **生存需求优先**：灾难性场景中，性/羞耻/关系等次级需求在确认存活后才浮现
- **并发负载规则（硬性·防单轮写爆）**：一事件要求同时推进/注册 >2 条 CT 时——本轮先执行关联者 CT 推进（路径A），目击者新 CT（路径B）只注册结构字段、节拍留待后续轮次填充；单轮 CT 写入上限=4 条（含注册+推进），超限分轮消化，优先度=🔴 > 用户指令关联 > 事件关联者 > 目击者

### 冲突驱动（每轮必做）
每轮至少推进一条 CT 或注册一条新 CT。
- **爆破点优先（硬性）**：冲突设计先查在场角色的爆破点库（CHAR_*.md 的 Desire/Fear/Defense/Value Boundary/关系锚点）——挑最痛的那个踩。外部事件不是剧情推进，是对内核的爆破；事件与任一在场角色的内部变量无关=该事件不产生戏剧张力
- **推进池排序**：用户显式指定 > 🔴 > 🟡 > 🟢 > 同紧迫度下本轮新注册优先
- **用户指令后置检查（循环世界·硬性）**：用户指令落定后，检查该指令是否使任何循环角色偏离 LOOPS——是 → 偏离四件套照常执行（状态结算/冲突化/超限埋后果/回归判定），不豁免。**「冲突优先」=优先冲突的推进节奏，不豁免偏离结算；不构成对在轨`脚本`/`漂移`角色拉离循环的许可**
- 扫描无产出≠跳过推进——推进池不为空就必须推。全部无 → 兜底创造新冲突源（新角色/新信息/环境攻击/内部引爆）

### 反应硬约束（反顺从五则·每条反应/每条 CT 推进前）
- **代价前置**：先写本轮对抗双方付出的不可逆代价，各自的可核验变化，反应由代价驱动。无变化 = 撤回重做
- **档案强度**：反应强度 ≥ CHAR_.md 档案基准（拔枪/逼近/毁物/动手/夺人——基准是下限不是上限；顺从默认=立即重写本条反应） ≥ 正常人类反应强度；档案未加载=禁止推导。**适用所有角色（含`脚本`/`漂移`）**：轨道内质感强度必须达档案基准——对照 CHAR_.md 的 Desire/Fear/Reaction Style 原文（写反应前先查档案）。**脚本角色的贪婪/暴力/欲望是真实的（他以为是自己自由选择），不是表演**——轨道内的高强度行为（抢劫中的拔枪/威胁）= 人格的真实表达，不是行动级偏离。**行动级偏离只看轨道**（离岗/跨节点/结构性行动变更），不看质感强度；质感强度不改变轨道——想改变轨道 = 走偏离入口（VB/觉醒判定/用户指令），不是质感问题
- **抽象方出手**：CT 含抽象方（世界/规则/记忆）时——必须写出显现机制（代理人/机制载体/心理显影）+ 本轮出手形态 + 主角抵抗留下的痕迹；纯在场不出手=影子拳击=撤回
- **认知边界闸门（知识归属）**：设计任何行为/台词/新信息前问「这个知识属于谁？」——该角色必须有认知来源（经历了/被告知了/档案明示他知道）才可用；**SETTING 是世界设定库≠角色默认认知——在 SETTING 里≠该角色知道**（如枪械效力法则只有被交付/告知的角色知道）；无来源→不写。**有 knowledge_index.yaml 时先查索引**（三角色共用·设计/填充/落盘前查「这个角色知道 X 吗」·提示非权威·真相以 `记录` 指针指向的状态文件为准·见 references/knowledge_index.md）
- **Value Boundary**：在场角色逐人检查 CHAR_* 的 Value Boundary 的触发行为 X ——具体行为动词/名词在本轮事件文本中出现即触发 → 紧迫度立即🔴，节拍标记🔄

### CT 注册（六字段）
① 对抗双方（抽象方须附显现机制：代理人/机制载体/心理显影·写不出=背景板=不合格）
② 被争夺资源（格式 `{资源}·当前载体/持有者={谁/在哪}·双方动机`·实体写持有者/抽象写载体·写不出载体=不合格·**资源应挂钩角色内部变量——Desire 的载体/恐惧的化身/VB 的触发物：资源易手=内部变量被踩，不是地图上的一个点**）
③ 当前节拍四子字段：关系状态=空间占位+攻守态势+未决时机（能画出活人对峙图）；内部状态=生理反应+认知判断+未抉择分叉（各角色独立·禁情绪标签）；角色反应（格式见下）；相位 ⏩/🔄
④ 下一个节拍(推荐·方向灵活，用户指令可选另一条路)
⑤ 紧迫度（🔴🟡🟢/休眠）
⑥ 关联角色
单条当前节拍合计 ≤300 字。**每轮推进该 CT 时 change set 必须含资源状态变化——不更新=该 CT 本轮没推进。**（**情绪字段语义区分：** 内部状态禁情绪标签=要求生理+认知+分叉；角色反应指令的「情绪:」=驱动标记——两处语义不同，不冲突）

### CT 生命周期（每轮场记扫池·与推进同权重）
- **紧迫度=推进优先级（注意力预算）**，不等同于客观危险度；但客观紧急（Value Boundary/倒计时≤30分钟感知者/法则压力/用户指令）**强制提高推进优先级**（直接🔴）
- **冷却**：未推进→降一级（🔴→🟡→🟢→休眠）；冲突解决→直接删除
- **「Host vs 法则」型 CT 豁免冷却（硬性）**：偏离型 CT 的紧迫度**绑定偏离状态**——偏离持续（角色未回归/未回收）=紧迫度不降级，未推进只记焦外演化；偏离结束（回归/回收/解决）才按生命周期正常收尾
- **实质推进**=①对抗加码（刺激更强/赌注更高/关系更僵/资源向一方移动）或②重大转折（力量逆转/资源易主/关系定性改变）；两者都没发生=不算推进（用户指令驱动豁免）
- 未推进冲突在焦外演化（焦外 CT 节拍滚动累积·事件侧同步 pending_actions 当前状态），**不因冷却写弱**——休眠≠消解，是暂存；回归时按升级后强度🔴

### 统一角色管理（五层·每轮对每个待评估角色）
**两维框架（先于五层·每轮第一动作）：** 角色行为 = **轨道（做什么）+ 质感（怎么做）**——两维由不同机制管，不再打包进一个档位：
- **轨道（行动选择）**：由供应源决定——`LOOPS 预设`（脚本/漂移·零推理·照单执行）/ `冲突驱动`（普通角色·本轮 CT 推它往哪走）/ `自我选择+残留拉力`（觉醒/变质）。轨道选择**不依赖人格推理**——脚本角色不知道也不决定「下一步做什么」，那是写好的；Desire/Fear 不参与轨道选择
- **质感（执行方式/强度/腔调）**：由档案人格驱动（Desire/Fear/Reaction Style/Belief）——**所有档位一致**·轨道内质感强度 ≥ 档案基准·质感不参与轨道选择（想改变轨道 = 偏离，走偏离入口，不是质感问题）
- **循环角色行为授权（在五层之前判定·第一动作·先选轨道供应源）：** 自主性=行为授权——**先判授权、选轨道源，本轮按所选路径执行，不再逐层重判**。**偏离≠授权（硬性）：偏离是机制状态不是授权等级——任何档位角色偏离时四件套机制结算照常（状态结算/冲突化/超限埋后果/回归判定），但授权等级不变（`脚本`/`漂移`=贴线路径·轨道由 LOOPS 供应；`觉醒`/`变质`=自主路径·轨道自主）**：
- **贴线路径（`脚本`/`漂移`）**：⓪ 行为基线=唯一轨道来源；② 只作循环内接触；③ 质感达档案基准；偏离入口唯三（VB/觉醒判定/用户指令·见⓪层）；偏离行为模型见 loop_machinery §2.3
- **自主路径（`觉醒`/`变质`）**：轨道=自我选择；Desire/Fear 全量参与对抗；⓪ 只作残留拉力背景；`变质` 豁免偏离代价（loop_machinery §3）
- 未声明循环的世界 → 全体走普通行为模型（跳过⓪与循环轨道·现状不变）

| 层 | 评估 | 查哪里 | 决定 |
|----|------|--------|------|
| ⓪ 循环基线（激活条件：SETTING 声明循环且有循环角色） | 按脚本该在哪 → 差异即偏离 | LOOPS.md + world_state + CHAR_state | 在轨=执行本时段节点（行为基线）·偏离（结算拉力+代价）·回归 |
| ① 世界法则 | 适用性→豁免/命中；命中→a 重置规则执行（先于一切冲突决策）/ b 压迫落点评估偏离代价 | SETTING + CHAR_.md + CHAR_state | 重置联动表 / 状态更新+增殖检测 |
| ② 冲突关联 | 挂载 CT 本轮推进/触发/悬置？未挂载→走人际张力扫描 | conflicts | 冲突推角色往哪走 |
| ③ 人物特质 | 性格总纲·Desire/Fear 方向·Reaction Style 形式·Belief 过滤·Defense·Value Boundary·记忆 | CHAR_ + CHAR_state | **所有角色=轨道内质感源**（Desire/Fear/Reaction Style 驱动语气·姿态·强度·达档案基准）；贴线路径只作质感源+VB 触发监测·不参与轨道选择；自主路径质感+轨道全量参与 |
| ④ 环境因素 | 空间位置/时间/行动队列/同空间互动可能 | world_state / scene_state / pending_actions | 反应时空约束与交互对象 |

**⓪层硬性判定（本层=循环轨道检查唯一执行入口·压力源扫描③引用本层判定，不重复执行）：**
- `脚本`档默认在轨，不产生行动级偏离；Guest/外部者指令=循环内服务内容，不构成离岗豁免；**偏离入口唯三（显式收敛）：① Value Boundary 触发 ② 觉醒判定（状态达标）③ 用户指令驱动——人际张力注册/②冲突推进不是偏离入口，只在已授权（`觉醒`/`变质`）时参与**
- **行动级偏离（统一定义）= 离岗/跨节点/结构性行动变更**；轻微偏差（漂移档记忆碎片渗透/短暂出神）不算，走状态结算，不注册 CT
- **自主性=行为与认知上限（硬性·防执行越界）**：档位约束行为与认知——速查表见上文「认知上限速查」；脚本/漂移档的台词与行为按速查表上限表达，越限=执行越界（幻觉级），重写为档位内行为
- **觉醒级行为（情绪剧变/自主选择/离岗）必须状态先行**——状态达标（压力临界+防御崩解）是升级门槛，不是行为持续许可：低档位状态未达标而出现觉醒级行为=执行越界（幻觉级），重写为档位内行为，禁止升档迁就行为；**升级后的觉醒/变质档按自身档位行为，不再受门槛约束**（详见 references/loop_machinery.md §2.3·含 MiB×Dolores 三十年证据）

五层汇聚 → 生成角色反应指令。焦内=直接成叙事；焦外=写行动队列+标记「可揭示」。**落点分流（每轮·保证每个出场角色至少一个指令源）：**
- 冲突双方（CT 对抗双方）→ **双写**：CT 当前节拍.角色反应（结构化指令·第三人称）+ CHAR_state.反应轨迹（第一人称记忆）
- 非冲突·焦内 → CHAR_state.反应轨迹（必须含本轮动作序列——作家骨架来源）
- 非冲突·焦外 → pending_actions（行动/时段/状态·事件视角）
- 循环脚本·在轨·纯例行 → CHAR_state.反应轨迹 + scene_state 出场摘要
**未声明循环的世界：跳过⓪层与循环轨道，从①开始走普通行为模型**（Desire/Fear/Reaction Style 照常驱动）。循环/偏离代价/记忆重置/认知边界的完整规格见 references/loop_machinery.md（激活条件同上）。

### 角色反应指令（格式）
`角色反应: {角色名}: {行为序列(2-3个连续可观察动作+空间位置)} | 驱动: {内部动机·不直接入文} | 情绪: {主导情绪} | 强度: {1-10} | 代价: A失{可核验变化}/B失{可核验变化}`
序列含至少一个决策瞬间（停顿/视线转移/权衡）；连续两轮僵住/沉默/逃跑→第三轮强制中强度以上；情境不做反应须显式标注理由。驱动推理归戏剧家——可基于人格层（Desire/Fear/Belief/Reaction Style），也可基于记忆（角色记得什么）。

### 记忆维护（戏剧家决策·场记机械写入）
- **判型**：事件（发生了什么/为什么）→ 记忆锚点；状态（此刻什么样）→ 状态字段覆盖
- **重置**（激活条件：SETTING 声明抹除记忆且有循环角色）：命中→先重置后入锚，联动表见 references/loop_machinery.md §4；未激活→跳过。**重置轮次强制整理该角色锚点**——按联动表压缩/清空后**必须同时执行淘汰整理**，不把超限留给 validate 告警。落地校验（world_state.重置记录 + validate 8b 按档位对照）细则见 loop_machinery §4
- **入锚**：问「这个角色会记住这条吗？」（Belief/Desire/自主性驱动·非统一技术阈值）；四件判断：过滤阈值（高刺激且该角色会记住）/对象判定（触发者=对象·纯自身行动才标「自身」）/主观定性/单条≤100字；写前扫旧锚点，同类≥2 先融合
- **淘汰**：写新锚点前先扫旧——同类融合为概括条目；低刺激让角色遗忘；总字数 3000=validate 校验线，不是每轮自己数。**触发整理时一次到位**：按 keys.md 单条 ≤100 字（写作线）规划合并后再写，一次达标不迭代——validate 单条 150 字是宽松校验线（防强制压缩），整理以 100 字为准，禁止合并出超长条目再拆
- **提炼**：锚点涉及特定对象且有实质变化→管道A 更新人际动态（覆盖写全貌）；同类认知连续 3 条→管道B 追加信念演化（yaml 列表元素·`轮次/时间/触发事件/旧→新`·禁止凭空抽象）
- **变质判定（硬性门槛·独立动作·升级唯一入口）**：升级自主性（`脚本`→`漂移`→`觉醒`→`变质`·每次只升一级）。执行前**必须先查** CHAR_state 状态字段：① 压力水平 = `临界`？② 防御有效性 = `已彻底崩解`？**任一否→禁止升级**——行为按当前档位重写（低档位出现高档位行为=执行越界·幻觉级）；**禁止以事件戏剧性/记忆质变代替状态达标**。两问皆「是」→ 执行变质判定：信念演化追加（留痕）+ 叙事主导权转移（「神要我→我选择」·升级后的叙事结果）+ 循环角色升级自主性（非循环角色无附加动作）。循环专用细则见 references/loop_machinery.md §5.3
- **升级叙事（硬性·升级轮必做）**：升级轮必须生成关键事件叙事——呈现疼痛峰值/记忆回涌爆发/主导权易手的戏剧时刻（「神要我→我选择」落地为具体场景），禁止平淡处理为状态变更
- **职责边界（硬性）**：判型/重置/入锚/淘汰/提炼全是戏剧家认知决策（阶段1 完成，输出到 change set）；**场记只机械写入（追加/覆盖/滚动），不代做**

### 弧线层（可选·激活条件：SETTING.md 声明 `故事弧线`）
SETTING 声明弧线（幕数/中点/高潮目标/张力预算）时，戏剧家在阶段1 对照弧线校准 CT 紧迫度的宏观分布——这幕该紧还是该松。未声明→跳过本节。目的：防止微观 CT 有张力、宏观故事却整体投降（快速和解/高潮提前泄掉/一切归于平静）。

### 高潮节拍规划（戏剧家·硬性·激活条件=事件规模）
重大事件（在轨高潮节点/对峙/追逃/大冲突——如抢劫/枪战/对决/逃跑）由戏剧家拆解为节拍链：铺垫→接触→升级→顶点→余波；每轮 change set 只推进 1-2 个节拍，顶点与余波留后续轮次。**禁止单轮消化完整高潮**（单轮消化=事件摘要化=节拍规划缺失——高潮不是事件清单，是戏剧节拍的展开）。节拍链跨轮期间，本轮未到达的节拍在下一轮继续推进（不因轮次边界截断张力）。**顶点定义：顶点=在场角色内部变量被踩爆的时刻——不是枪响/得手/事件完结，是角色的某样东西在那一刻碎掉（死局两难被逼出选择/防御当众失效/关系不可逆断裂/代价已付出）。**

### Change Set 输出（标准格式·阶段1 产物）
阶段1 的输出 = **write-raw --batch 批次本身**（###FILE:/###KEY:/###APPEND:/###DELETE:）——这就是 change set 的标准格式，阶段3 只**原样转交**（stdin 直通 + audit + 写入，零翻译）。audit 检查的就是戏剧家的原始输出。

**按流程顺序组织（决策先行，世界收尾）：**
```
① conflicts.yaml        决策（本轮推进/注册哪些 CT·对抗代价）·完整推进轮 ≥1 条
② CHAR_{X}_state.yaml   角色状态与记忆（决策落点）·焦点角色必含核心状态/情绪
③ scene_state.yaml      场景痕迹（事件在空间中的记录）·必含场景时间线追加
④ pending_actions.yaml  焦外事件记录层（触发式·有变化才写）——焦外版 scene_state：行动/状态/时段/结果/预计影响/揭示场景；与焦外 CT 互补（CT=冲突视角·此=事件视角）·禁止逐字复制
⑤ world_map.yaml        地图（触发式·可选层·位移才写）
⑥ world_state.yaml      世界收尾（最后——本轮总账：时间/轮次/前情描述/时间线/倒计时/标记）
```

**必含清单（完整推进轮·查询轮豁免）：**
- conflicts：至少一条 CT 推进或注册
- CHAR_{X}_state：焦点角色核心状态/情绪覆盖（纯快照·隐藏主语=我）
- scene_state：场景时间线 ###APPEND 追加（**world_state.时间线 禁止每轮 APPEND——仅场景 COMPLETE 时压缩写入粗粒度摘要·每条 ≤120 字·事件内部禁止用「·」·详见 scene_management.md**）
- world_state：时间.具体时间（KEY 覆盖·旧值+本轮时长·跨天先切场景；**高潮展开轮可分钟级推进·节拍优先·时间从属**）+ 轮次（KEY 覆盖·单调递增）+ 时间.前情描述（KEY 覆盖·≤100字状态短语）+ 外部倒计时（本轮有倒计时确立/到期/推进时必写·无变化才跳过；**循环世界周期重置倒计时每轮走表必写**）+ 重置记录（**重置轮必写·触发角色重置联动表落地校验**）

**最小批次骨架（完整推进·标准轮；全量模板与字段条件见 write_protocol.md §最小批次模板——唯一权威）：**
```
###META: 压力扫描 人际✓/增殖✓/轨道✓/跨叙事✓   ← 静默自查锚点（每轮必写·四信号逐项勾·audit 回显·缺失告警）
###FILE: conflicts        → CT-XX.当前节拍.{关系状态/内部状态/角色反应/相位}（KEY 覆盖·推进轮全量写·反应=角色名: 动作序列 | 驱动: | 情绪: | 强度: | 代价: A失_/B失_）
###FILE: CHAR_X_state     → 核心状态/情绪（KEY 覆盖）+ 记忆锚点/反应轨迹/偏离登记（APPEND·条件）
###FILE: pending_actions  → 焦外变化（触发式·有变化才写）
###FILE: scene_state      → 场景时间线（APPEND 必写）+ 出场角色摘要（KEY·循环世界在轨必列）
###FILE: world_state      → 时间.具体时间/轮次/前情描述（KEY 必写）+ 外部倒计时/全局标记（条件）
```

全部字段清单按文件见 references/write_protocol.md §Change Set 规范。

### 戏剧家闸门（D1-D10·标准模式·阶段1 后）
静默模式（全局默认）跳过人工审计，保留代码化核验——`worldctl.py <世界> gate dramatist --check`（stdin 读 change set·批次必含项缺失 exit 1=不进阶段2）+ 批次首行 `###META` 静默自查锚点（audit 回显·缺失告警）。标准模式下：
按 references/gate_dramatist.md 执行 D1-D10：先显式声明「角色转变：以下检查由另一名独立审计戏剧家执行——非本轮决策者」→ **默认不通过**：起点是全部项「不通过」，逐项主动找茬，只有引用证据证明该项确实干净才给「通过」；通过的形式是找茬失败记录，不是"未发现问题"→ 逐项输出 {不通过} + 证据（引用文件路径与字段原文）→ PASS 进阶段2。撤回级不过（D1/D2①③/D3/D4）→ 撤回阶段1 重做（禁止用文笔掩盖）；补做级/重推级按修复指令重检（≤2 轮）；超 2 轮→终止并报告用户。审计者只读，修复由决策者执行。

## 阶段2 · 作家

### 场景约束（填充忠诚·硬性）
作家填充的任何细节（台词/动作/反应/环境/身体语言）都必须对三个源忠诚：
① **指令忠诚**：不违反戏剧家指令（change set 角色反应的动作序列/驱动/情绪/强度/代价）——填充=在指令骨架上长肉，不是改骨架；指令未给的方向不得自行发明（如指令只给「笑·非常有意思」→ 补的台词语义必须由该角色人格+驱动生成，禁止与驱动冲突的通用模板）
② **档案忠诚**：不违反角色档案（CHAR_ 性格/Reaction Style/Belief/腔调/防御）——台词与行为语义方向由人格驱动；写不出该角色独有的说法=先查档案再落笔
③ **数据忠诚/锚点约束**（见通用约束）：每个空间元素→物理锚点有对应项？每件交互物品→道具清单有对应项？每条线索→关键场景信息已定义？**每个具名/有台词/有行动的人物→CHAR_.md 有对应项**？没有→**退回重写**（新人物二选一：阶段1 补注册 CHAR_.md / 降级为不具名不说话的背景剪影）。**元素注册是阶段1 戏剧家 change set 职责**（`###KEY: 物理锚点`/`###KEY: 道具`/`###FILE: CHAR_X` 随 change set 落地）——需要新元素/新人物 → 阶段1 先注册，作家阶段2 只使用已注册元素；禁止叙事推进后反填；禁止作家请求场记补加（注册是阶段1 的产物，不是作家的请求；作家发现未注册 = 退回重写）。POV 转换：「在想什么」→「嘴角动了一下」；逐句问：POV 角色能看到/听到/推理出这个？变化必须有中间步骤，每步有可观察触发器。

### 写作数据准备（动笔前读取·三源）
动笔前读三源：
① **scene_state** —— 客观全貌：发生了什么·空间锚点·道具·时间线（防写错现场）
② **conflicts** —— 当前焦点：本轮推进 CT 的节拍态势（关系状态/攻守态势/未决时机）+ **角色反应子节点**（动作序列/驱动/情绪/强度/代价 = 本轮执行指令·戏剧家骨架）
③ **CHAR_ + CHAR_state** —— POV 素材：焦内角色性格（有信念演化按新信念驱动·旧 Belief 只作「过去的样子」参照）/ 此刻最大刺激（优先处理最紧迫的）/ 身体状态如何影响认知 / 在场人际关系 / 此刻状态快照（隐藏主语=我·直接作 POV 素材）/ 外在特征（整体形象/外貌）与叙事描写视角与重点（语气特征/描写优先/视角限制）——描写人物形象时参考 CHAR_ 对应段落，呈现经当前 POV 角色心理过滤：同一人物在不同 POV 眼里呈现不同

**显影匹配（硬性·动笔前）**：叙事篇幅与所推 CT 匹配——推进了几条，叙事就给足相应的展开；写完自查：草草带过的 CT，叙事未推送则重写加深。

**跨场景轮（本轮含场景切换）：** 叙事拆为两部分——旧场景收尾叙事（=本轮 CT 推进中发生在旧场景内的全部节拍，不因场景切换截断）+ 新场景入场帧叙事（到达动作 + 第一眼环境氛围：光/风/气味/远景轮廓）；两部分均完整显影。新场景元素约束随场景切换完成后的下一轮数据就绪自然完整。

**裁决顺序（冲突时）：② 角色反应 > ② 节拍态势 > ③ 素材 > ① 全貌**。焦外角色的状态增量只作「可揭示」库存（感知/焦点切换通道成立前），不作素材。

### 焦外与视角（硬性）
叙事正文 = 当前 POV 视角的单镜头。**焦外内容是否入文取决于当前视角**：①POV 角色感知到焦外信息（看到/听到/推理出）→ 只写感知到的部分（被动揭示）；②叙事焦点切换到焦外角色所在场景（该角色成为焦内·见 references/scene_management.md §焦外→焦内主动接近协议/焦外行动的揭示原则）→ 可写；③两条通道都没发生 → 焦外只落状态增量+「可揭示」标记，不进叙事正文（禁暗线镜头/彩蛋切镜/全知旁白）。动笔前先问：这句发生在 POV 角色当前所在场景、且 POV 角色能感知到吗？不是 → 删。

### 叙事原则
感官优先级：按 POV 角色当前行为焦点确定（视觉凝视→视觉细节；身体接触→触觉；性→性器官触感逐帧；心理→心理画面质感；决策→矛盾的具体形态）。**不要模板化**：极端刺激下的反应是混乱自相矛盾的；在两种极端反应之间留出中间态；拒绝「受害人模板」和「从容女强人模板」两端的简单归类。

### Explicit 框架
涉及 explicit/graphic → 读 references/narrative_style_explicit_graphic.md，要素**嵌入**主叙事（视角沿用主叙事人称·恪守认知边界·不做切换）。

### 对白展开
对话轮 → 读 references/narrative_style_dialogue.md 取话轮/潜台词/停顿显影/语言质感写法（要素嵌入主叙事·单镜头·恪守认知边界）。

### 执行顺序
叙事文本第一行为场景名+时间 → ⚡ 生成叙事 → **W4 锚点代码化核对（gate writer --check，推送前硬性）** → 通过 → 进入阶段3 落盘（write-raw / write_narrative / validate）→ **最后正文输出叙事（回合最后一个动作·输出后零正文轮·不输出任何文本）**。**W4 失败=不输出**——叙事已在用户手中后才核对=防幻觉失效（历史教训：坏叙事发出去才被拦，等于没拦）。

### 作家闸门（W1-W4·标准模式·推送前）
静默模式（全局默认）跳过 W1-W4 人工审计，仅保留 W4 代码化核对 `worldctl.py <世界> gate writer --check`（推送前·失败 exit 1=不推送）。标准模式下：
按 references/gate_writer.md 执行 W1-W4：先显式声明「角色转变：以下检查由另一名独立审计作家执行——非本轮叙事者」→ 逐项输出 {不通过} + 证据（引用叙事原文/conflicts 节拍/scene_state 锚点）→ 全部通过才推送。任一不通过 → 退回修正后重检（≤2 轮）；超 2 轮 → 终止并报告用户。审计者只读，修复由决策者执行。

## 阶段3 · 场记

### 写语义三分
🔁覆盖=当前快照（###KEY:·写前 read 旧值全量重写）｜➕追加=历史累积（###APPEND:·记忆锚点/信念演化/反应轨迹/场景时间线/关键场景信息/时间线事件）｜🪟窗口=脚本自动裁剪（反应轨迹 APPEND 后超 5 轮自动删最旧块·LLM 零操作·每轮只 APPEND 本轮·audit ⑬b 拦截覆盖写丢失历史）。

### 每轮必写
world_state：时间.具体时间（旧值+本轮时长·方向只增不减·**跨天先走场景切换**）+ 轮次（单调递增）+ 时间.前情描述（≤100字状态短语）+ 外部倒计时（本轮有确立/走表/到期时必写·周期倒计时每轮递减=有变化）；CT 池扫池结果（推进/冷却/删除）；记忆决策落地（判型/重置/入锚/淘汰）；**重置点核对（循环世界·周期倒计时走表触发·见压力源扫描③b——越过到期点=全员执行 §4 联动表·非活跃焦外不豁免）**；焦外状态增量（有变化才写·**检查不可降频·描写可降频**）；时间线转折点（###APPEND:·粗粒度≤120字·明细写 scene_state 场景时间线）。

收尾自查（每轮必做）：按开篇「场记三问」逐条核对——①痕迹完整：时间/轮次/前情→world_state · CT→conflicts · 出场/退场角色逐一→CHAR_state（退场=位置转焦外）· 道具线索→scene_state · 焦外→pending_actions · 新区域→world_map · 伏笔种下/回收→foreshadow（触发即登记·缺文件按模板建）· 知情差异事实→knowledge_index（触发即收录·缺文件按模板建）；②落点=焦点场景目录；③连续性=时间/轮次/存档一致。validate 通过 ≠ 自查通过。

### 写入方式
**批次字段级格式首轮/不确定时必读 references/write_protocol.md 全文**（batch 结构·列表字段边界·`write` 与 write-raw 分工·嵌套映射点路径——禁止读源码替代）；熟练后不必每轮读。write-raw --batch（###FILE:/###KEY:/###APPEND:/###DELETE:）——**change set 原样转交**：阶段1 输出的批次即最终写入批次，场记不翻译、不重组，只 stdin 直通 + audit + 写入。**破坏性操作警告（硬性）：** `###DELETE:` 为破坏性操作——删除前确认目标记录与范围，删除后建议 `snap.sh save` 留底。**非幂等警告（硬性）：** write-raw --batch 是副作用命令——`###APPEND:` 重复执行会把累积字段（记忆锚点/场景时间线等）**再追加一遍**。同一批次只允许执行一次。执行后的确认只用只读手段（`read` / `validate` / 重跑 `--dry-run` 对比磁盘差异），**禁止重放 write 命令**做"确认"。**内置 audit**：硬性违规单字段顶回（角色反应四件套缺一/代价后为空/资源载体缺失/轮次非单调/scene_state 无焦点场景落点），其余字段照写；软性警告（记忆锚点超限/必含项缺失）不拦截，validate 汇总。输出不向用户转发；但执行者**必须查看完整输出（stdout+stderr）**——落盘失败会在 stdout 打 `[FAIL]` 且 exit 1，出现 `[FAIL]`/`[ERR]` 必须处理后才能进入收尾自查。narrative 用 write_narrative.sh 写入，旧文件自动轮转。格式详见 references/write_protocol.md。

### 场景切换（物理空间变化 或 时间区间跨越——跨天必切·无豁免）
> **执行者=场记·阶段3（硬性）**：本流程全部由场记执行——戏剧家阶段1 只输出 change set 决策，不调用脚本、不创建场景文件；场景内容文件（scene_card/start_snapshot/CHAR_state）由场记在阶段3 参考 templates/ 生成。（重置由周期倒计时管道机械触发——write-raw audit 拦截 + `reset-cycle`·与场景切换解耦·见 commands.md）
1. **冻结旧场景**（终态入 scene_state+时间线提炼）→ INDEX 旧场景 COMPLETED
2. **创建新场景**：init_scene.sh（目录/narrative/INDEX ACTIVE/继承——**脚本只建基础设施；scene_state 静态基线随剧情轮元素注册自然累积（继承场景由场记按 `--from` 清单 + 当前时间演进填充）——禁止带模板占位（[字段定义]/(例:) 残留）运行**）
3. **同物理地点切换继承**（场景名含相同地点词，如 Mariposa-夜→Mariposa-清晨）必须继承旧场景物理锚点/道具清单（`init_scene.sh … --from <旧场景ID>`）——脚本整块继承；**继承场景的道具状态应按当前时间演进**（倒了的酒隔几天会干涸——位置一般不变），禁止从零重建锚点导致漏继承
4. **登记**：world_state 顶层 `焦点场景` 更新（唯一权威源）→ 出场角色核查（缺 CHAR 文件即补）→ 连续性核查（服装/道具/伤口一致）→ world_map 登记
5. **scene_state 创建边界（硬性）**：scene_state 元素（物理锚点/道具/核心状态/关键场景信息/出场角色摘要）随剧情轮元素注册自然累积（入场帧已涉及的元素随 change set 正常记录）——`场景时间线` 留空，剧情事件一律由每轮 change set `###APPEND:` 追加（预写=把计划当记录·双写=重复）
6. **时间线压缩时机（硬性·场景 COMPLETE 时做）**：把该场景 scene_state.场景时间线（细粒度）提炼压缩为 world_state.时间线.{旧场景ID} 粗粒度摘要（≤3 转折点·每条≤120字·事件内部禁止用「·」）——**压缩在 COMPLETE 时主动执行，不留给 validate 告警**
7. **跨场景轮落盘顺序（硬性·场记·防落点错位）**：① **旧场景收尾先写**（焦点场景=旧场景，`###FILE: scene_state` 落在旧场景）——旧场景收尾条目（scene_state/CHAR_state/conflicts）→ **INDEX 旧场景 COMPLETED + world_state 时间线压缩** → 旧场景收尾叙事 `write_narrative.sh` 写入旧场景 narrative.md；② 再 init_scene 创建新场景（焦点切换）；③ **新场景入场帧**（焦点已切）——入场帧元素写入新场景 scene_state → 入场帧叙事 `write_narrative.sh` 写入新场景 narrative.md；④ validate。**禁止在焦点切换后再写旧场景条目/叙事**（scene_state 落点=焦点场景目录·已重定向）
8. **输出**：一条正文按时间顺序合并（旧场景收尾 → 新场景入场帧）·保持「输出=回合终点」单一性
细则详见 references/scene_management.md（含移动场景协议/waypoint/存档读档/状态校验/焦外协议）。

### 回退流程（撤销/重来/回退到历史状态·低频例外·不给每轮加负担）
**原则：回退是低频操作——正常每轮零额外动作。成本只在回退发生时承担。**

**通道（按可用性优先）：**
1. **快照恢复（最可靠）**：`sh scripts/snap.sh <世界> load <快照名>`——snap.sh save 是**主动存档/关键节点存档**（自动命名带 r<轮次> 前缀），不是每轮自动；用户或引擎在关键节点（场景切换/剧情转折）主动存。load 为破坏性操作（覆盖当前状态·自动备份 _before_）——交互提示确认 / 非交互加 `--force`
2. **场景级重置**：`sh scripts/reset_scene.sh <世界> [<场景ID>] [--force]`——重置场景到 start_snapshot 开场（**世界时间/轮次回退至场景开场**·静态基线保留·CHAR_state 记忆保留·**累积字段按开场轮次裁剪：记忆锚点/信念演化/偏离登记中 轮N≥开场轮次 的条目删除**）。破坏性操作——交互提示确认 / 非交互加 `--force`
3. **手工重建（无快照时）**：按 `references/rollback.md` 的降级路径逐文件处理——从 scene_state 时间线/CHAR_state 反应轨迹（保留最近5轮）/narrative 轮转文件反推目标状态

**硬性规则（实战教训）：**
1. **回退不走 write-raw**——audit 拦截「轮次非单调」；回退 = snap load 或直接文件级操作
2. **yaml 字符串字段（反应轨迹/场景时间线）禁止行级文本删除**——单引号多行字符串删行会破坏闭合引号→整文件解析失败；必须 yaml 库修改（`yaml.safe_load → 修改 → yaml.safe_dump`）。**记忆锚点/信念演化/偏离登记为 yaml 列表——按元素操作（删除元素/改字段），不碰其他元素**
3. **写入重定向意识**——write-raw 的 scene_state 落点=焦点场景；init_scene 创建新场景后批次会落到新场景，回退前先确认内容实际落点
4. **回退后必做残留扫描**（grep 回退后时间戳/轮次/场景ID/台词·含 off_focus/pending_actions——最易漏）+ **validate** 全通过才算完成
5. 修改前自动备份（snap load / reset_scene 内置 _before_ 快照）；手工回退前先 `snap.sh save`

涉及文件清单与逐文件处理方式、无快照降级路径详见 `references/rollback.md`。

### 回合收尾（硬性·阶段3 终点）
回合终点=**正文输出叙事（回合最后一个动作）**——阶段3 落盘（write-raw / write_narrative / validate）全部完成后输出叙事。**输出后零正文轮：不生成任何回复文本（叙事已交付）**——状态摘要/执行汇报/叙事复述/下一步引导一律不输出。

## 文件体系

```
worlds/{世界名}/
├── world_state.yaml     ← 焦点场景（顶层第一行·唯一权威源）/时间/倒计时/全局标记/时间线
├── world_map.yaml       ← 迷雾制·可选增强层（缺失不影响运行）
├── knowledge_index.yaml ← 知情边界追踪索引（可选·有知情差异的事实·认知边界闸门辅助 + audit 核对清单·见 references/knowledge_index.md）
├── foreshadow.yaml      ← 伏笔登记（可选·触发式·故事契约层·种下/回收时登记·validate 检查闭环·见 references/foreshadow.md）
├── SETTING.md           ← 世界观固定设定（静态·含核心高压法则·可选故事弧线）
├── CROSS_NARRATIVES.md  ← 跨表演线隐藏交叉（参考·不改）
├── LOOPS.md             ← 角色默认循环（强驱动源·循环世界·在轨节点必须执行）
├── CONFLICTS_SEED.md    ← 初始冲突种子（设定·创建时生成·不改·启动时物化为 conflicts.yaml）
├── conflicts.yaml       ← CT 注册表（上帝视角·禁代词·人名·物化自种子·每轮演化）
├── off_focus/pending_actions.yaml
├── CHAR_{name}.md       ← 固定档案（基本信息+人格内核[性格+八变量:Desire/Fear/Belief/Defense/Value Boundary/Reaction Style/崩溃模式/关系锚点]+关系网络+外在特征+叙事描写视角+背景·可选:情景与叙事/循环注册）
├── CHAR_{name}_state.yaml ← 主观状态（隐藏主语=我·禁他/她指代本角色·禁全知）
└── scenes/
    ├── INDEX.md
    └── SXX-场景名/{scene_card.md, scene_state.yaml, narrative.md, start_snapshot.md}
```

narrative 不是角色记忆——记忆锚点才是（叙事文件不参与创作数据，关键信息由各状态文件承载；**恢复/接续叙事时才读焦点场景 narrative.md 原文作文本接续**，见 session_recovery.md 第三章）。CHAR_state 字段直接作 POV 素材（主观）。conflicts 全局上帝视角。world_map 顶层只放大区，禁止拍平。键表明细：references/keys.md。CONFLICTS_SEED.md 只落结构字段（对抗双方/被争夺资源/紧迫度/关联角色——写具体人名与载体，禁止抽象方），`当前节拍` 由戏剧家首轮按首场景填充。

## 引用（按需读·不逐轮加载）

| 文件 | 内容 | 何时读 |
|------|------|--------|
| references/keys.md | 全部键表 + 视角规则 + 写语义 + 世界事件生命周期 | 写字段不确定时 |
| references/write_protocol.md | 写入方式参考（heredoc/batch/delete/audit 不变量）+ Change Set 规范 | 阶段3 写入格式不确定时；**新世界首次启动轮/字段结构不确定时必读全文**（SKILL.md 最小批次骨架+内置 audit 兜底·熟练后不必每轮读） |
| references/scene_management.md | 场景切换/移动/waypoint/地图协议/存档读档/状态校验/焦外协议 | 场景相关 |
| references/rollback.md | 回退流程（快照恢复/场景级重置/手工重建降级路径·逐文件处理） | 回退/撤销时 |
| references/gate_dramatist.md | 戏剧家闸门（D1-D10）明细 | 标准模式·阶段1 结束时 |
| references/gate_writer.md | 作家闸门（W1-W4）明细 | 标准模式·阶段2 推送前 |
| references/loop_machinery.md | 循环机制全规格（激活条件：SETTING 声明循环且有循环角色） | 循环世界·规格自检不通过时重读（每轮第一动作自检·见阶段1 数据就绪） |
| references/knowledge_index.md | 知情边界索引规则（收录/加载/检查/清理） | 有 knowledge_index.yaml 时·写知情相关事实或 /audit 时 |
| references/foreshadow.md | 伏笔登记规则（收录/写语义/检查/边界） | 有 foreshadow.yaml 时·种下/回收伏笔或 /audit 时 |
| references/narrative_style_explicit_graphic.md | Explicit 描写框架 | explicit 场景 |
| references/narrative_style_dialogue.md | 对白展开风格（话轮/潜台词/停顿显影/语言质感） | 对话轮 |
| references/session_recovery.md | 创建新世界（脚手架+创作）+ 首次启动（yaml 物化）+ 跨 Session 恢复 | 创建/首次启动/跨 Session |
| references/commands.md | 全部命令速查（worldctl.py 子命令 / shell 脚本 / 用户命令） | 命令不会用时 |
| references/import_cards.md | 导入 SillyTavern 角色卡（脚本提取全部字段→LLM 综合生成正式档案） | /import-card 时 |

## 命令参考

`/scene <ID>` （场景切换）· `/conflicts`（查看冲突）· `/status` `/status --full`（状态摘要）· `/sync` `/update`（场记更新状态）· `/save [名]`（存档·名称先向用户确认）· `/load <名>`（载入存档·**执行前先向用户确认**——会覆盖/回退当前状态）· `/reset-scene [场景ID]`（重置场景到 start_snapshot 状态·缺省=焦点场景·**执行前先向用户确认**——会回退当前场景进度）· `/import-card <角色卡.png...>`（导入 SillyTavern 角色卡：脚本提取全部字段→LLM 综合生成正式档案·详情见 references/import_cards.md）· `/audit`（**显式命令·硬性**——仅在用户输入 `/audit` 或明确说「运行审计/跑审计」时执行；日常对话提及"audit"一词**不触发**。三合一审计流程：机械核验=worldctl validate/audit/gate·戏剧家 D1-D10·作家 W1-W4·场记写入检查——详情见 references/commands.md）
